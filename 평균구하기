class Solution {
  public double solution(int[] arr) {
      double answer = 0;
      int res = 0;
      for(int i=0;i<arr.length;i++)
          res += arr[i];
      answer = (double)res/arr.length;
      return answer;
  }
}
